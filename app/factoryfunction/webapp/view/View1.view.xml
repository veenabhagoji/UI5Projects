<mvc:View controllerName="factoryfunction.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
    <Table id="idTable" items="{ path: 'Products>/Products', factory: '.factoryFun' }">

    	<columns>
			<Column
				width="12em">
				<Text text="Product" />
			</Column>
			<Column
				minScreenWidth="Tablet"
				demandPopin="true"
                hAlign="Center">
				<Text text="Supplier" />
			</Column>
			<Column
				minScreenWidth="Desktop"
				demandPopin="true"
				hAlign="End">
				<Text text="Dimensions" />
			</Column>
			<Column
				minScreenWidth="Desktop"
				demandPopin="true"
				hAlign="Center">
				<Text text="Weight" />
			</Column>
			<Column
				hAlign="End">
				<Text text="Price" />
			</Column>
		</columns>
			<!-- <items> -->
			<!-- <ColumnListItem vAlign="Middle"> -->
				<!-- <cells> -->
					<!-- <ObjectIdentifier
						title="{Products>Product}"
                        text="{Products>ProductId}"
						/>
					<Text
						text="{Products>SupplierName}" />
					<Text
						text="{Products>Dimensions}" />
                        <Text text="{Products>Weight}" />
                        <Text text="{Products>Price}" /> -->
					
					
				<!-- </cells> -->
			<!-- </ColumnListItem> -->
		<!-- </items> -->
    </Table>
    </Page>
</mvc:View>